/** @description Check if a given number is even or odd
 * @param {number} number
 * @returns {boolean}
 */

const numberIsEven = number => {
  console.log(number % 2 === 0)
  return number % 2 === 0
}

export default numberIsEven
